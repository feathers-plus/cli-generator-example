
// Define TypeScript interface for service `relationships`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// !<DEFAULT> code: nedb_interface
export interface Relationship {
  _id: string;
  uuid: number;
  followerUuid: number;
  followeeUuid: number;
}
// !end

// !code: funcs // !end
// !code: end // !end
