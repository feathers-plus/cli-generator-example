
// Define TypeScript interface for service `posts`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// !<DEFAULT> code: nedb_interface
export interface Post {
  _id: string;
  uuid: number;
  authorUuid: number;
  body: string;
  draft: number;
}
// !end

// !code: funcs // !end
// !code: end // !end
