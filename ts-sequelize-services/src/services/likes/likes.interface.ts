
// Define TypeScript interface for service `likes`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// !<DEFAULT> code: nedb_interface
export interface Like {
  id: number;
  uuid: number;
  authorUuid: number;
  commentUuid: number;
}
// !end

// !code: funcs // !end
// !code: end // !end
